#include <stdio.h>
int main() {
   //add clearscreen feature
   clrscr();
   // printf() displays the string inside quotation
   printf("Hello, World!");
   // add goodbye text
   printf("Goodbye, World!");
   return 0;
}